# Hádej číslo (Guess the Number)

Simple console game written in Python.

The program chooses a random number between 1 and 100 and the player tries to guess it.

## How to run

```bash
python hadejkdo.py
